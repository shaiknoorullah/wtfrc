# wtfrc v0.1 "Ask" Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a local-first CLI tool that indexes dotfiles/configs, stores them in a semantic SQLite knowledge base, and answers natural-language questions via a Bubble Tea popup REPL powered by a local LLM.

**Architecture:** Go binary with Cobra CLI, Viper config, modernc.org/sqlite for the knowledge base with FTS5, Bubble Tea TUI for the interactive REPL, and an LLM abstraction layer supporting Ollama and OpenAI-compatible APIs. The indexer parses configs via tool-specific parsers, enriches them with an LLM, and stores semantic entries. The responder searches the KB via FTS5, feeds matches to a fast local LLM, and streams the answer.

**Tech Stack:** Go 1.22+, Cobra, Viper, Bubble Tea, Lip Gloss, Bubbles, Glamour, modernc.org/sqlite, GoReleaser

**Spec:** `docs/design/2026-03-16-wtfrc-architecture.md`

---

## File Structure

```
wtfrc/
├── cmd/wtfrc/main.go                    # Entry point — initializes Cobra root
├── internal/
│   ├── config/config.go                 # Viper TOML config loading + defaults
│   ├── config/config_test.go
│   ├── kb/
│   │   ├── db.go                        # SQLite schema, migrations, open/close
│   │   ├── db_test.go
│   │   ├── models.go                    # KBEntry, Session, Query, UsageEvent structs
│   │   ├── search.go                    # FTS5 query logic
│   │   └── search_test.go
│   ├── indexer/
│   │   ├── indexer.go                   # Orchestrates discovery → parse → enrich → store
│   │   ├── indexer_test.go
│   │   ├── manifest.go                  # Change detection (SHA-256 vs DB manifest)
│   │   ├── manifest_test.go
│   │   ├── redactor.go                  # Secret stripping
│   │   ├── redactor_test.go
│   │   └── parsers/
│   │       ├── parser.go               # Parser interface + RawEntry + EntryType
│   │       ├── registry.go             # Parser registry (all parsers register here)
│   │       ├── i3.go                   # i3/sway bindsym parser
│   │       ├── i3_test.go
│   │       ├── hyprland.go
│   │       ├── hyprland_test.go
│   │       ├── tmux.go
│   │       ├── tmux_test.go
│   │       ├── kitty.go
│   │       ├── kitty_test.go
│   │       ├── shell.go                # zsh/bash aliases, functions, exports
│   │       ├── shell_test.go
│   │       ├── git.go
│   │       ├── git_test.go
│   │       ├── ssh.go
│   │       ├── ssh_test.go
│   │       ├── nvim.go
│   │       ├── nvim_test.go
│   │       ├── vscode.go
│   │       ├── vscode_test.go
│   │       ├── systemd.go
│   │       ├── systemd_test.go
│   │       └── generic.go             # Fallback line-by-line parser
│   ├── llm/
│   │   ├── provider.go                 # Provider interface + types
│   │   ├── ollama.go                   # Ollama implementation
│   │   ├── ollama_test.go
│   │   ├── openai_compat.go            # OpenAI-compatible implementation
│   │   ├── openai_compat_test.go
│   │   ├── json.go                     # CompleteJSON helper with retry
│   │   ├── json_test.go
│   │   ├── fallback.go                 # Fallback chain for offline mode
│   │   └── fallback_test.go
│   ├── tui/
│   │   ├── ask.go                      # Bubble Tea model for REPL
│   │   ├── styles.go                   # Lip Gloss theme
│   │   └── components/
│   │       ├── input.go                # Query input with history
│   │       └── answer.go              # Streaming answer viewport
│   ├── session/
│   │   ├── manager.go                  # Session lifecycle (create, log query, close)
│   │   ├── manager_test.go
│   │   ├── archive.go                  # JSONL archival
│   │   └── archive_test.go
│   ├── supervisor/
│   │   ├── supervisor.go               # Review logic + hallucination detection
│   │   ├── supervisor_test.go
│   │   └── report.go                   # Report generation
│   └── cli/
│       ├── root.go                     # Root command + global flags
│       ├── ask.go                      # wtfrc ask
│       ├── index.go                    # wtfrc index
│       ├── search.go                   # wtfrc search
│       ├── list.go                     # wtfrc list
│       ├── supervise.go                # wtfrc supervise
│       ├── stats.go                    # wtfrc stats
│       ├── doctor.go                   # wtfrc doctor
│       ├── config_cmd.go              # wtfrc config
│       └── setup.go                   # wtfrc setup (first-run experience)
├── configs/default.toml                # Default config template
├── scripts/install.sh                  # curl installer
├── go.mod
├── go.sum
├── .goreleaser.yml
├── Makefile
├── LICENSE
└── README.md
```

---

## Chunk 1: Project Scaffold + Config + Knowledge Base

### Task 1: Initialize Go Module and Dependencies

**Files:**
- Create: `go.mod`
- Create: `cmd/wtfrc/main.go`
- Create: `Makefile`

- [ ] **Step 1: Initialize Go module**

Run: `cd /home/devsupreme/work/wtfrc && go mod init github.com/shaiknoorullah/wtfrc`

- [ ] **Step 2: Add core dependencies**

Run: `go get` for each of the following packages at latest: `github.com/spf13/cobra`, `github.com/spf13/viper`, `modernc.org/sqlite`, `github.com/charmbracelet/bubbletea`, `github.com/charmbracelet/lipgloss`, `github.com/charmbracelet/bubbles`, `github.com/charmbracelet/glamour`, `github.com/charmbracelet/log`, `github.com/google/uuid`.

- [ ] **Step 3: Create minimal main.go**

Create `cmd/wtfrc/main.go` in package `main`. It MUST import `github.com/shaiknoorullah/wtfrc/internal/cli` and call `cli.Execute()` in the `main` function. If `Execute()` returns an error, print it to stderr and exit with code 1.

- [ ] **Step 4: Create root command stub**

Create `internal/cli/root.go` in package `cli`. Define a package-level Cobra root command variable with `Use: "wtfrc"`, `Short: "AI that reads your dotfiles so you don't have to"`, and a longer description. Export an `Execute() error` function that calls `rootCmd.Execute()`.

- [ ] **Step 5: Create Makefile**

Create a `Makefile` with four phony targets: `build` (compiles to `bin/wtfrc` via `go build`), `test` (runs `go test ./... -v -count=1`), `lint` (runs `go vet ./...`), and `clean` (removes the `bin/` directory).

- [ ] **Step 6: Verify it compiles and runs**

Run: `go build -o bin/wtfrc ./cmd/wtfrc && ./bin/wtfrc --help`
Expected: Cobra help output with "wtfrc" description.

- [ ] **Step 7: Commit**

Commit message: `feat: initialize Go module with Cobra CLI scaffold`

---

### Task 2: Config Loading

**Files:**
- Create: `internal/config/config.go`
- Create: `internal/config/config_test.go`
- Create: `configs/default.toml`

- [ ] **Step 1: Write the failing test**

Create `internal/config/config_test.go` in package `config` with two test functions:

**TestLoadDefaults:** Call `Load("")` with an empty path (no config file). It MUST return defaults without error. Assert that `cfg.LLM.Fast.Provider` equals `"ollama"`, `cfg.LLM.Fast.Model` equals `"gemma3:4b"`, `cfg.Popup.MaxContextEntries` equals `10`, and `cfg.Session.RetainDays` equals `90`.

**TestLoadFromFile:** Write a temporary TOML file that overrides `[llm.fast]` to use provider `"openai-compat"`, model `"llama-3.1-8b-instant"`, with a Groq base URL and API key env, and sets `[popup] max_context_entries = 5`. Call `Load(cfgPath)` and assert the overridden values match. Also assert that unset values still have their defaults (e.g., `cfg.Session.RetainDays` MUST still be `90`).

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/config/ -v`
Expected: FAIL — `Load` function not defined.

- [ ] **Step 3: Implement config loading**

Create `internal/config/config.go` in package `config`. Define the following structs, all with `mapstructure` tags matching the TOML field names:

- **Config** with fields: General (GeneralConfig), Indexer (IndexerConfig), LLM (LLMConfig), Popup (PopupConfig), Session (SessionConfig), Supervisor (SupervisorConfig), Privacy (PrivacyConfig).
- **GeneralConfig** with field: AssistantName (string).
- **IndexerConfig** with fields: ScanPaths ([]string), AutoDiscover (bool), ExcludePaths ([]string), Watch (bool), ReindexSchedule (string).
- **LLMConfig** with fields: Fast (LLMProviderConfig), Strong (LLMProviderConfig).
- **LLMProviderConfig** with fields: Provider (string), Model (string), BaseURL (string), APIKeyEnv (string).
- **PopupConfig** with fields: Frontend (string), MaxContextEntries (int), MaxHistory (int).
- **SessionConfig** with fields: RetainDays (int), ArchiveFormat (string).
- **SupervisorConfig** with fields: Enabled (bool), Schedule (string), ModelTier (string), RetainReports (int).
- **PrivacyConfig** with fields: RedactPatterns ([]string), NeverIndex ([]string).

Implement a `setDefaults` function that registers Viper defaults for all fields. Key defaults: assistant_name `"wtfrc"`, auto_discover `true`, watch `false`, reindex_schedule `"daily"`, fast LLM provider `"ollama"` with model `"gemma3:4b"`, strong LLM provider `"openai-compat"` with model `"claude-sonnet-4-20250514"` and Anthropic base URL, popup frontend `"term"` with max_context_entries `10` and max_history `5`, session retain_days `90` with archive_format `"jsonl"`, supervisor enabled with daily schedule and strong model_tier, retain_reports `30`, privacy redact_patterns `["sk-", "xoxb-", "ghp_", "password", "secret", "token"]`, never_index `["~/.ssh/id_*", "~/.gnupg/", "~/.aws/credentials", "**/.env"]`.

Implement `Load(path string) (*Config, error)` that creates a new Viper instance, sets config type to `"toml"`, calls `setDefaults`, optionally reads the config file if a path is provided, and unmarshals into a `Config` struct.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/config/ -v`
Expected: PASS

- [ ] **Step 5: Create default.toml template**

Copy the TOML config from spec §11 into `configs/default.toml`.

- [ ] **Step 6: Commit**

Commit message: `feat: add TOML config loading with Viper defaults`

---

### Task 3: Knowledge Base — SQLite Schema + Models

**Files:**
- Create: `internal/kb/models.go`
- Create: `internal/kb/db.go`
- Create: `internal/kb/db_test.go`

- [ ] **Step 1: Create models.go with Go structs**

**Note:** `EntryType` is defined once in `internal/indexer/parsers/parser.go` (Task 5). All other packages import it from there to avoid type identity issues.

Create `internal/kb/models.go` in package `kb` with the following structs:

- **KBEntry** with fields: ID (int64), Tool (string), Type (parsers.EntryType), RawBinding (*string, nullable), RawAction (*string, nullable), Description (string), Intents ([]string), SourceFile (string), SourceLine (int), Category (string), SeeAlso ([]string), IndexedAt (time.Time), FileHash (string).
- **Session** with fields: ID (string), StartedAt (time.Time), EndedAt (*time.Time, nullable), Queries ([]Query), ModelUsed (string).
- **Query** with fields: ID (int64), SessionID (string), Question (string), Answer (string), EntriesUsed ([]int64), ResponseTimeMs (int64), Timestamp (time.Time), AccuracyScore (*float64, nullable), Issues ([]string).
- **ManifestEntry** with fields: FilePath (string), SHA256 (string), Mtime (time.Time), LastIndexed (time.Time), EntryCount (int).

- [ ] **Step 2: Write the failing test for DB open + schema**

Create `internal/kb/db_test.go` in package `kb` with two test functions:

**TestOpenCreatesSchema:** Open a new DB in a temp directory. Query `sqlite_master` to verify these tables exist: `entries`, `intents`, `manifest`, `sessions`, `queries`, `usage_events`, `supervisor_runs`. Also verify these FTS5 virtual tables exist: `intents_fts`, `entries_fts`.

**TestInsertAndGetEntry:** Open a new DB. Create a KBEntry with tool `"i3"`, type `EntryKeybind`, binding `"$mod+Shift+q"`, action `"kill"`, description `"Close the focused window"`, source file, source line 42, category `"window_management"`, file hash. Insert it with intents `["close window", "kill window", "how to close"]`. Verify the returned ID is non-zero. Call GetEntry with that ID and verify tool, description, and that there are exactly 3 intents.

- [ ] **Step 3: Run test to verify it fails**

Run: `go test ./internal/kb/ -v`
Expected: FAIL — `Open`, `InsertEntry`, `GetEntry` not defined.

- [ ] **Step 4: Implement db.go**

Create `internal/kb/db.go` in package `kb`. Define a `DB` struct wrapping a `*sql.DB` connection.

**Open(path string) (*DB, error):** Open a SQLite connection using `modernc.org/sqlite` with WAL journal mode and foreign keys enabled (via pragma query params). Run migrations on open. Return the wrapped DB.

**Close() error:** Close the underlying connection.

**Conn() *sql.DB:** Expose the raw connection for packages that need direct access.

**migrate() error:** Execute the full DDL schema as a single `Exec` call. The schema MUST create these tables (all with `IF NOT EXISTS`):

- **entries:** id (INTEGER PK AUTOINCREMENT), tool (TEXT NOT NULL), type (TEXT NOT NULL), raw_binding (TEXT nullable), raw_action (TEXT nullable), description (TEXT NOT NULL), source_file (TEXT NOT NULL), source_line (INTEGER), category (TEXT), see_also (TEXT, JSON-encoded), indexed_at (TEXT NOT NULL), file_hash (TEXT NOT NULL).
- **intents:** id (INTEGER PK AUTOINCREMENT), entry_id (INTEGER NOT NULL FK to entries with CASCADE delete), phrase (TEXT NOT NULL).
- **intents_fts:** FTS5 virtual table on phrase, content-synced with intents table.
- **entries_fts:** FTS5 virtual table on description, content-synced with entries table.
- Triggers for both FTS5 tables: AFTER INSERT, AFTER DELETE, and AFTER UPDATE triggers to keep FTS in sync.
- **manifest:** file_path (TEXT PK), sha256 (TEXT NOT NULL), mtime (TEXT NOT NULL), last_indexed (TEXT NOT NULL), entry_count (INTEGER NOT NULL DEFAULT 0).
- **sessions:** id (TEXT PK), started_at (TEXT NOT NULL), ended_at (TEXT nullable), query_count (INTEGER DEFAULT 0), model_used (TEXT), archived (INTEGER DEFAULT 0).
- **queries:** id (INTEGER PK AUTOINCREMENT), session_id (TEXT NOT NULL FK to sessions), question (TEXT NOT NULL), answer (TEXT NOT NULL), entries_used (TEXT, JSON-encoded), response_time_ms (INTEGER), timestamp (TEXT NOT NULL), accuracy_score (REAL nullable), issues (TEXT nullable).
- **usage_events:** id (INTEGER PK AUTOINCREMENT), tool (TEXT NOT NULL), action_type (TEXT NOT NULL DEFAULT ''), action (TEXT NOT NULL), optimal_action (TEXT nullable), entry_id (INTEGER FK to entries nullable), timestamp (TEXT NOT NULL), was_optimal (INTEGER NOT NULL DEFAULT 0), coached (INTEGER DEFAULT 0), time_saved_ms (INTEGER nullable).
- **supervisor_runs:** id (INTEGER PK AUTOINCREMENT), run_at (TEXT NOT NULL), sessions_reviewed (INTEGER), issues_found (INTEGER), optimizations_applied (TEXT), model_used (TEXT).
- Indexes on: entries(tool), entries(type), intents(entry_id), queries(session_id), usage_events(tool), usage_events(timestamp).

**InsertEntry(e KBEntry, intents []string) (int64, error):** Insert the entry into `entries`, get the last insert ID, then insert each intent phrase into `intents` linked by entry_id. JSON-encode the `SeeAlso` field. Use RFC3339 for timestamps.

**GetEntry(id int64) (*KBEntry, error):** Query the entry by ID, scan all fields (parsing time and JSON fields), then query and attach all intents for that entry.

**DeleteEntriesByFile(filePath string) error:** Delete all entries matching the source_file. FK cascades MUST handle intent cleanup.

- [ ] **Step 5: Run tests**

Run: `go test ./internal/kb/ -v`
Expected: PASS

- [ ] **Step 6: Commit**

Commit message: `feat: add SQLite knowledge base with FTS5 schema and entry CRUD`

---

### Task 4: FTS5 Search

**Files:**
- Create: `internal/kb/search.go`
- Create: `internal/kb/search_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/kb/search_test.go` in package `kb`. Include a `seedTestDB` helper that opens a fresh DB and inserts two entries: an i3 keybind (`$mod+Shift+q` / `kill` / `"Close the currently focused window"` with intents `["close window", "kill window", "quit window"]`) and a tmux keybind (`prefix + [` / `copy-mode` / `"Enter copy mode for scrolling and text selection"` with intents `["scroll in tmux", "copy mode", "select text tmux"]`).

**TestSearchByIntent:** Search for `"close window"` with limit 5. MUST return at least one result. The first result's Tool MUST be `"i3"`.

**TestSearchByDescription:** Search for `"scroll"` with limit 5. MUST return at least one result. The first result's Tool MUST be `"tmux"`.

**TestSearchNoResults:** Search for `"kubernetes deploy"` with limit 5. MUST return zero results.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/kb/ -v -run TestSearch`
Expected: FAIL — `Search` method not defined.

- [ ] **Step 3: Implement search.go**

Create `internal/kb/search.go` in package `kb`. Define a `SearchResult` struct that embeds `KBEntry` and adds a `Score float64` field.

**Search(query string, limit int) ([]SearchResult, error):** Implement a CTE-based SQL query that searches both `intents_fts` and `entries_fts`. Intent matches SHOULD be weighted higher than description matches (e.g., intent score = rank * -1.0, description score = rank * -0.5). Combine results with a UNION ALL, deduplicate by entry_id taking the MAX score, join back to the entries table, and ORDER BY score DESC with LIMIT. For each result, also load its intents from the intents table.

Before passing the query to FTS5, implement an `ftsEscape` helper that wraps each whitespace-delimited word in double quotes so FTS5 treats them as literals. Internal double quotes MUST be escaped by doubling them.

If the FTS5 MATCH fails (e.g., on empty or invalid query), return an empty result set rather than an error.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/kb/ -v`
Expected: All PASS

- [ ] **Step 5: Commit**

Commit message: `feat: add FTS5 search across intents and descriptions`

---

## Chunk 2: Parser Framework + First 4 Parsers

### Task 5: Parser Interface + Registry

**Files:**
- Create: `internal/indexer/parsers/parser.go`
- Create: `internal/indexer/parsers/registry.go`

- [ ] **Step 1: Create parser interface and types**

Create `internal/indexer/parsers/parser.go` in package `parsers`.

Define an `EntryType` as a `string` type with the following constants: `EntryKeybind` ("keybind"), `EntryAlias` ("alias"), `EntryFunction` ("function"), `EntryExport` ("export"), `EntrySetting` ("setting"), `EntryService` ("service"), `EntryHost` ("host").

Define a `RawEntry` struct with fields: Tool (string), Type (EntryType), RawBinding (string), RawAction (string), SourceFile (string), SourceLine (int), ContextLines (string).

Define a `Parser` interface with three methods: `Name() string`, `CanParse(path string) bool`, and `Parse(path string) ([]RawEntry, error)`.

- [ ] **Step 2: Create registry**

Create `internal/indexer/parsers/registry.go` in package `parsers`.

Define a package-level `registry` slice of `Parser`. Implement `Register(p Parser)` to append a parser to the registry. Implement `ForFile(path string) Parser` that returns the first parser whose `CanParse` returns true for the given path, or nil if none match. Implement `All() []Parser` that returns all registered parsers.

- [ ] **Step 3: Commit**

Commit message: `feat: add parser interface and registry`

---

### Task 6: i3/sway Parser

**Files:**
- Create: `internal/indexer/parsers/i3.go`
- Create: `internal/indexer/parsers/i3_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/indexer/parsers/i3_test.go` in package `parsers` with two test functions:

**TestI3ParserCanParse:** Instantiate an `I3Parser` and verify that `CanParse` returns true for paths ending in `/i3/config` and `/sway/config` (at any depth), and false for paths like `kitty.conf` or shell scripts inside the i3 directory.

**TestI3ParserParse:** Write a temp file containing sample i3 config with: a `set $mod Mod4` line, several `bindsym` lines (e.g., `$mod+Shift+q kill`, `$mod+Return exec kitty`, `$mod+1 workspace number 1`, `$mod+2 workspace number 2`), a `bindcode` line, and a non-keybind `exec` line. Parse the file and assert exactly 5 entries are returned. Verify the first entry has binding `"$mod+Shift+q"`, action `"kill"`, and type `EntryKeybind`. Verify the bindcode entry has the correct binding.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/indexer/parsers/ -v -run TestI3`
Expected: FAIL — `I3Parser` not defined.

- [ ] **Step 3: Implement i3.go**

Create `internal/indexer/parsers/i3.go` in package `parsers`. Register the parser via an `init` function.

Define `I3Parser` as an empty struct implementing the `Parser` interface. `Name()` returns `"i3"`. `CanParse(path)` returns true when the path ends with `/i3/config` or `/sway/config`.

`Parse(path)` MUST read the file line by line, skip comments and blank lines, and match lines against a regex for `bindsym` and `bindcode` directives. The regex MUST capture the key binding (everything after bindsym/bindcode up to the action) and the action (the remainder). Each match produces a `RawEntry` with type `EntryKeybind`, the tool name derived from the path (either `"i3"` or `"sway"`), and up to 3 lines of surrounding context.

Implement an `appendContext` helper that maintains a sliding window of the last N lines for context.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/indexer/parsers/ -v -run TestI3`
Expected: PASS

- [ ] **Step 5: Commit**

Commit message: `feat: add i3/sway config parser`

---

### Task 7: Shell Parser (zsh/bash)

**Files:**
- Create: `internal/indexer/parsers/shell.go`
- Create: `internal/indexer/parsers/shell_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/indexer/parsers/shell_test.go` in package `parsers` with two test functions:

**TestShellParserCanParse:** Verify the parser accepts `.zshrc` and `.bashrc` paths, and rejects non-shell config paths like `kitty.conf`.

**TestShellParserParse:** Write a temp `.zshrc` file containing 3 aliases (single-quoted, e.g., `alias k='kitty'`), 2 exports (e.g., `export EDITOR=nvim`), 2 functions (one using `function name()` syntax, one using `name()` shorthand), and a non-matching line (e.g., `setopt auto_cd`). Parse the file and use a `filterType` helper to separate entries by EntryType. Assert: exactly 3 aliases, 2 exports, 2 functions. Verify the first alias has binding `"k"` and action `"kitty"`.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/indexer/parsers/ -v -run TestShell`
Expected: FAIL

- [ ] **Step 3: Implement shell.go**

Create `internal/indexer/parsers/shell.go` in package `parsers`. Register via `init`.

Define `ShellParser` implementing `Parser`. `Name()` returns `"shell"`. `CanParse(path)` returns true when the base filename is `.zshrc`, `.bashrc`, `.bash_profile`, or `.zprofile`.

`Parse(path)` MUST read line by line, skip comments and blanks, and match against four regex patterns in order of priority:

1. **Alias (single-quoted):** matches `alias name='value'`, captures name as binding and value as action, type `EntryAlias`.
2. **Alias (double-quoted):** matches `alias name="value"`, same capture, type `EntryAlias`.
3. **Export:** matches `export NAME=value`, captures NAME as binding and value (stripped of surrounding quotes) as action, type `EntryExport`.
4. **Function (keyword):** matches `function name()`, captures name as binding, type `EntryFunction`.
5. **Function (shorthand):** matches `name() {`, captures name as binding, type `EntryFunction`.

The tool name SHOULD be `"zsh"` or `"bash"` based on whether the filename contains "bash".

- [ ] **Step 4: Run tests**

Run: `go test ./internal/indexer/parsers/ -v -run TestShell`
Expected: PASS

- [ ] **Step 5: Commit**

Commit message: `feat: add zsh/bash shell parser for aliases, functions, exports`

---

### Task 8: tmux Parser

**Files:**
- Create: `internal/indexer/parsers/tmux.go`
- Create: `internal/indexer/parsers/tmux_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/indexer/parsers/tmux_test.go` in package `parsers`.

**TestTmuxParserParse:** Write a temp `tmux.conf` containing: a `set -g prefix C-a` line, and five bind lines using various forms: `bind-key r source-file ~/.tmux.conf`, `bind -r h select-pane -L`, `bind -r j select-pane -D`, `bind [ copy-mode`, `bind -T copy-mode-vi v send -X begin-selection`. Parse and assert exactly 5 entries. The first entry MUST have binding `"r"` and action `"source-file ~/.tmux.conf"`.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/indexer/parsers/ -v -run TestTmux`
Expected: FAIL

- [ ] **Step 3: Implement tmux.go**

Create `internal/indexer/parsers/tmux.go` in package `parsers`. Register via `init`.

Define `TmuxParser` implementing `Parser`. `Name()` returns `"tmux"`. `CanParse(path)` returns true for paths ending in `tmux.conf` or `.tmux.conf`.

`Parse(path)` MUST match lines against a regex for `bind` and `bind-key` directives. The regex MUST handle optional flags (`-r`, `-n`, `-T <table>`) before the key binding. Capture the key as binding and everything after as the action. Tool is always `"tmux"`, type is always `EntryKeybind`.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/indexer/parsers/ -v -run TestTmux`
Expected: PASS

- [ ] **Step 5: Commit**

Commit message: `feat: add tmux config parser`

---

### Task 9: kitty Parser

**Files:**
- Create: `internal/indexer/parsers/kitty.go`
- Create: `internal/indexer/parsers/kitty_test.go`

- [ ] **Step 1: Write the failing test**

Create `internal/indexer/parsers/kitty_test.go` in package `parsers`.

**TestKittyParserParse:** Write a temp `kitty.conf` containing: a `font_size 12.0` line, four `map` directives (e.g., `map ctrl+shift+c copy_to_clipboard`, `map ctrl+shift+v paste_from_clipboard`, `map ctrl+shift+t new_tab`, `map ctrl+shift+enter new_window`), and a non-map setting (`scrollback_lines 10000`). Parse and assert exactly 4 entries. The first entry MUST have binding `"ctrl+shift+c"`.

- [ ] **Step 2: Run test to verify it fails**

Run: `go test ./internal/indexer/parsers/ -v -run TestKitty`
Expected: FAIL

- [ ] **Step 3: Implement kitty.go**

Create `internal/indexer/parsers/kitty.go` in package `parsers`. Register via `init`.

Define `KittyParser` implementing `Parser`. `Name()` returns `"kitty"`. `CanParse(path)` returns true for paths ending in `kitty.conf`.

`Parse(path)` MUST match lines against a regex for `map` directives, capturing the key combo as binding and the action name as action. Tool is always `"kitty"`, type is always `EntryKeybind`.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/indexer/parsers/ -v -run TestKitty`
Expected: PASS

- [ ] **Step 5: Commit**

Commit message: `feat: add kitty config parser`

---

## Chunk 3: Remaining Parsers (6 more) + Secret Redactor

### Task 10: Hyprland Parser

**Files:** `internal/indexer/parsers/hyprland.go`, `internal/indexer/parsers/hyprland_test.go`

- [ ] **Step 1: Write failing test** — parse `bind = $mainMod, Q, killactive`, `bind = $mainMod SHIFT, E, exit`, `bind = $mainMod, Return, exec, kitty`
- [ ] **Step 2: Run test, verify FAIL**
- [ ] **Step 3: Implement** — regex for `bind\s*=\s*(.+?),\s*(\w+),\s*(.+)` pattern. Extract modifier+key as binding, action as everything after second comma.
- [ ] **Step 4: Run test, verify PASS**
- [ ] **Step 5: Commit**: `feat: add hyprland config parser`

### Task 11: Git Config Parser

**Files:** `internal/indexer/parsers/git.go`, `internal/indexer/parsers/git_test.go`

- [ ] **Step 1: Write failing test** — parse `[alias]` section with `co = checkout`, `st = status`, `lg = log --oneline --graph`
- [ ] **Step 2: Run test, verify FAIL**
- [ ] **Step 3: Implement** — detect `[alias]` section header, parse `key = value` lines until next section.
- [ ] **Step 4: Run test, verify PASS**
- [ ] **Step 5: Commit**: `feat: add git config alias parser`

### Task 12: SSH Config Parser

**Files:** `internal/indexer/parsers/ssh.go`, `internal/indexer/parsers/ssh_test.go`

- [ ] **Step 1: Write failing test** — parse Host blocks with hostname, user, identityfile (redacted). Type = `EntryHost`.
- [ ] **Step 2: Run test, verify FAIL**
- [ ] **Step 3: Implement** — state machine: track current Host block, collect key-value pairs. Redact IdentityFile values.
- [ ] **Step 4: Run test, verify PASS**
- [ ] **Step 5: Commit**: `feat: add SSH config parser`

### Task 13: nvim Keymap Parser

**Files:** `internal/indexer/parsers/nvim.go`, `internal/indexer/parsers/nvim_test.go`

- [ ] **Step 1: Write failing test** — parse `vim.keymap.set("n", "<leader>ff", ...)` patterns from lua files
- [ ] **Step 2: Run test, verify FAIL**
- [ ] **Step 3: Implement** — regex for `vim.keymap.set\(["'](\w)["'],\s*["']([^"']+)["'],\s*(.+?)[,)]`
- [ ] **Step 4: Run test, verify PASS**
- [ ] **Step 5: Commit**: `feat: add nvim lua keymap parser`

### Task 14: VS Code + Zed Keybinding Parsers

**Files:** `internal/indexer/parsers/vscode.go`, `internal/indexer/parsers/vscode_test.go`, `internal/indexer/parsers/zed.go` (minimal — shares JSON keybinding logic)

- [ ] **Step 1: Write failing test** — parse JSON keybinding array with `key`, `command`, `when` fields
- [ ] **Step 2: Run test, verify FAIL**
- [ ] **Step 3: Implement** — json.Unmarshal into `[]struct{Key, Command, When string}`, convert to RawEntry
- [ ] **Step 4: Run test, verify PASS**
- [ ] **Step 5: Commit**: `feat: add VS Code and Zed keybinding parsers`

### Task 15: systemd + generic Parsers

**Files:** `internal/indexer/parsers/systemd.go`, `internal/indexer/parsers/systemd_test.go`, `internal/indexer/parsers/generic.go`

- [ ] **Step 1: Write failing test** — parse systemd unit files for Description and ExecStart
- [ ] **Step 2: Run test, verify FAIL**
- [ ] **Step 3: Implement systemd parser** — INI-style parsing of `[Unit]` and `[Service]` sections
- [ ] **Step 4: Implement generic parser** — line-by-line fallback, each non-blank non-comment line becomes a `EntrySetting`
- [ ] **Step 5: Run tests, verify PASS**
- [ ] **Step 6: Commit**: `feat: add systemd and generic fallback parsers`

### Task 16: Secret Redactor

**Files:**
- Create: `internal/indexer/redactor.go`
- Create: `internal/indexer/redactor_test.go`

- [ ] **Step 1: Write failing test**

Create `internal/indexer/redactor_test.go` in package `indexer`.

**TestRedactSecrets:** Instantiate a `Redactor` via `NewRedactor([]string{"sk-", "xoxb-", "ghp_", "password", "secret", "token"})`. Test the following input/expected pairs:

- An export line containing an `sk-` prefixed API key MUST have the key value replaced with `[REDACTED]`.
- An `IdentityFile` line MUST have the path replaced with `[REDACTED_KEY_PATH]`.
- A PostgreSQL connection URL with an inline password MUST have the password replaced with `[REDACTED]` while preserving the URL structure.
- A Slack token starting with `xoxb-` MUST be fully redacted.
- A normal config line with no secrets MUST be returned unchanged.
- A bare GitHub personal access token starting with `ghp_` MUST be fully redacted.

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement redactor.go**

Create `internal/indexer/redactor.go` in package `indexer`.

Define a `Redactor` struct holding: a slice of compiled regex patterns (one per secret prefix), a compiled regex for SSH key paths, and a compiled regex for passwords in URLs.

**NewRedactor(secretPatterns []string) *Redactor:** For each secret pattern string, compile a case-insensitive regex that matches the pattern prefix followed by alphanumeric/underscore/hyphen characters. Also compile the SSH IdentityFile regex and URL password regex.

**Redact(s string) string:** Apply redactions in this order: (1) SSH key paths — replace the path after `IdentityFile` with `[REDACTED_KEY_PATH]`. (2) Passwords in URLs — replace the password portion in `://user:password@` format with `[REDACTED]`. (3) Pattern-based token redaction — replace each match of the compiled patterns with `[REDACTED]`. (4) Key=value pairs where the key name contains "password", "secret", "token", or "key" — replace the value with `[REDACTED]`.

**RedactFields(rawAction, contextLines *string):** Convenience method that applies `Redact` to both pointer arguments if non-nil.

- [ ] **Step 4: Run tests**

Run: `go test ./internal/indexer/ -v -run TestRedact`
Expected: PASS (some test cases may need tuning — adjust regex as needed)

- [ ] **Step 5: Commit**

Commit message: `feat: add secret redactor for API keys, SSH paths, URL passwords`

---

## Chunk 4: LLM Abstraction Layer

### Task 17: LLM Provider Interface + Types

**Files:**
- Create: `internal/llm/provider.go`

- [ ] **Step 1: Create provider.go with interface and types**

Create `internal/llm/provider.go` in package `llm`.

Define a `Provider` interface with four methods: `Name() string`, `Complete(ctx context.Context, req CompletionRequest) (CompletionResponse, error)`, `Stream(ctx context.Context, req CompletionRequest) (<-chan string, error)`, and `HealthCheck(ctx context.Context) error`.

Define a `Message` struct with fields Role (string, json:"role") and Content (string, json:"content").

Define a `ResponseFormat` string type with constants `FormatText` ("text") and `FormatJSON` ("json").

Define a `CompletionRequest` struct with fields: System (string), Messages ([]Message), MaxTokens (int), Temperature (float64), ResponseFormat (ResponseFormat).

Define a `TokenUsage` struct with fields: PromptTokens (int), CompletionTokens (int).

Define a `CompletionResponse` struct with fields: Content (string), Model (string), Usage (TokenUsage), LatencyMs (int64).

- [ ] **Step 2: Commit**

Commit message: `feat: add LLM provider interface and types`

### Task 18: Ollama Provider

**Files:**
- Create: `internal/llm/ollama.go`
- Create: `internal/llm/ollama_test.go`

- [ ] **Step 1: Write the test** — use httptest.NewServer to mock the Ollama /api/chat endpoint. Test Complete (returns full JSON), test Stream (returns chunked NDJSON), test HealthCheck.

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement ollama.go**

Key implementation details:
- `POST http://localhost:11434/api/chat` with `{"model": "...", "messages": [...], "stream": false}` for Complete
- For Stream: same endpoint with `stream: true`, read NDJSON lines, each has `{"message": {"content": "token"}}`
- HealthCheck: `GET http://localhost:11434/api/tags`
- Parse timing from response to populate LatencyMs

- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add Ollama LLM provider`

### Task 19: OpenAI-Compatible Provider

**Files:**
- Create: `internal/llm/openai_compat.go`
- Create: `internal/llm/openai_compat_test.go`

- [ ] **Step 1: Write the test** — mock OpenAI `/v1/chat/completions` endpoint. Test Complete, Stream (SSE `data:` lines), HealthCheck (`/v1/models`).

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement openai_compat.go**

Key details:
- Standard OpenAI chat completions format
- SSE streaming: lines starting with `data: `, parse JSON for `choices[0].delta.content`
- `data: [DONE]` signals end of stream
- API key from environment variable (specified by config's `api_key_env` field)

- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add OpenAI-compatible LLM provider`

### Task 20: CompleteJSON Helper

**Files:**
- Create: `internal/llm/json.go`
- Create: `internal/llm/json_test.go`

- [ ] **Step 1: Write the test** — mock provider returns valid JSON on first try: success. Mock returns garbage: retries, returns valid JSON: success. Mock returns garbage both times: error.

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement json.go**

Create `internal/llm/json.go` in package `llm`.

Implement a generic function `CompleteJSON[T any](ctx context.Context, p Provider, req CompletionRequest) (T, error)`. It MUST set `req.ResponseFormat = FormatJSON`, call `p.Complete`, and attempt to `json.Unmarshal` the response content into `T`. If unmarshalling fails, it MUST retry exactly once by appending the malformed assistant response and a user message requesting valid JSON only. If the second attempt also fails to unmarshal, return an error. If the provider itself errors on either call, propagate that error.

- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add CompleteJSON helper with retry on malformed output`

### Task 20b: Fallback Chain (Offline Mode)

**Files:**
- Create: `internal/llm/fallback.go`
- Create: `internal/llm/fallback_test.go`

Per spec §5.4: when the primary provider is unreachable, try fallback, then return a clear error. No silent degradation — the user always knows which model answered (or that none are available).

- [ ] **Step 1: Write failing test** — create a `FallbackProvider` wrapping primary + fallback. Test: primary healthy: uses primary. Primary down, fallback healthy: uses fallback. Both down: returns error with helpful message.

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement fallback.go**

Create `internal/llm/fallback.go` in package `llm`.

Define a `FallbackProvider` struct with fields Primary (Provider) and Fallback (Provider, may be nil). It MUST implement the `Provider` interface. For `Complete`, `Stream`, and `HealthCheck`: attempt the Primary first. On error, if Fallback is non-nil, delegate to it. If Fallback is nil or also fails, return an error message that names the unavailable provider(s).

- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add LLM fallback chain for offline resilience`

---

## Chunk 5: Indexer + Manifest + Enrichment

### Task 21: Manifest (Change Detection)

**Files:**
- Create: `internal/indexer/manifest.go`
- Create: `internal/indexer/manifest_test.go`

- [ ] **Step 1: Write failing test** — create a temp file, compute its hash, save to manifest DB, verify IsChanged returns false. Modify the file, verify IsChanged returns true. Delete the file, verify IsDeleted.

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement manifest.go**

Implement the following functions:

- **ComputeHash(path string) (string, error):** Compute and return the SHA-256 hex digest of the file at the given path.
- **IsChanged(path string) (bool, error):** Compare the current file's hash against the hash stored in the manifest table. Return true if the hashes differ or the file is not yet tracked.
- **Update(entry ManifestEntry) error:** Upsert the entry into the manifest table.
- **Remove(path string) error:** Delete the entry for the given path from the manifest table.
- **ListTracked() ([]ManifestEntry, error):** Return all entries currently in the manifest table.

- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add SHA-256 manifest for incremental change detection`

### Task 22: Indexer Orchestrator

**Files:**
- Create: `internal/indexer/indexer.go`
- Create: `internal/indexer/indexer_test.go`

- [ ] **Step 1: Write failing test**

Create `internal/indexer/indexer_test.go` in package `indexer`. Define a `mockEnricher` struct that implements the `Enricher` interface by returning canned enrichment data (description = `"mock description for "` + the binding, a single intent, and category `"test"`).

**TestIndexerFullPipeline:** Open a fresh DB, create a temp i3 config with 2 bindsym lines and a temp .zshrc with 2 aliases. Instantiate the indexer with the mock enricher and a redactor. Call `Index(ctx, paths)`. Verify that a search for `"kitty"` returns results. Verify the manifest reports i3 config as unchanged.

**TestIndexerIncremental:** Index a .zshrc with 1 alias. Modify the file to add a second alias. Re-index. Verify the new alias is searchable.

**TestIndexerDeletedFile:** Index a .zshrc. Delete the file. Re-index with empty paths. Verify the old entries are no longer searchable.

- [ ] **Step 2: Run test, verify FAIL**

Run: `go test ./internal/indexer/ -v -run TestIndexer`
Expected: FAIL — `New`, `Index` functions not defined.

- [ ] **Step 3: Implement indexer.go**

The Indexer orchestrates the full pipeline:
1. Discover files (from config scan_paths + auto_discover)
2. Check manifest for changes
3. For changed files: parse, redact, then batch for LLM enrichment
4. Store entries + intents in KB
5. Update manifest

Define an `Enricher` interface with method `Enrich(ctx context.Context, entries []parsers.RawEntry) ([]EnrichedEntry, error)`.

Define an `EnrichedEntry` struct with fields: Description (string), Intents ([]string), Category (string), SeeAlso ([]string).

Implement `New(db *kb.DB, enricher Enricher, redactor *Redactor) *Indexer` and `Index(ctx context.Context, paths []string) error`.

- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add indexer orchestrator with discovery, parsing, enrichment pipeline`

### Task 23: LLM Enricher

**Files:**
- Create: `internal/indexer/enricher.go`
- Create: `internal/indexer/enricher_test.go`

- [ ] **Step 1: Write failing test** — mock LLM provider returns valid enrichment JSON. Verify batching (30 entries: 1 batch, 60 entries: 2 batches). Verify JSON parsing of the LLM response.

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement enricher.go** — builds the enrichment prompt per spec §3.3 Stage 2, sends batches to the Strong LLM using CompleteJSON, returns EnrichedEntry slice.

- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add LLM enricher with batching for semantic entry generation`

---

## Chunk 6: Session Manager + Responder TUI

### Task 24: Session Manager

**Files:**
- Create: `internal/session/manager.go`
- Create: `internal/session/manager_test.go`

- [ ] **Step 1: Write failing test** — Create session, log 3 queries, close session. Verify session in DB with correct query_count, ended_at set. Verify queries retrievable. Also test `RecentQueries(sessionID, limit)` — log 6 queries, request last 3, verify only the 3 most recent are returned (this is the in-session context window from spec §7.2).

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement manager.go**

Implement the following functions:

- **NewManager(db *kb.DB) *Manager**
- **StartSession(modelUsed string) (*kb.Session, error):** Create a new session with a UUID, the current timestamp, and the model name.
- **LogQuery(sessionID string, q kb.Query) error:** Insert the query and increment the session's query_count.
- **EndSession(sessionID string) error:** Set the ended_at timestamp.
- **GetSession(id string) (*kb.Session, error):** Retrieve a session by ID with its queries.
- **RecentSessions(limit int) ([]kb.Session, error):** Return the N most recent sessions.
- **RecentQueries(sessionID string, limit int) ([]kb.Query, error):** Return the N most recent queries for a session (for in-session context window).

- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add session manager for query lifecycle tracking`

### Task 25: Session Archiver

**Files:**
- Create: `internal/session/archive.go`
- Create: `internal/session/archive_test.go`

- [ ] **Step 1: Write failing test** — archive sessions older than N days to JSONL, verify file written, verify DB rows marked archived.
- [ ] **Step 2: Run test, verify FAIL**
- [ ] **Step 3: Implement** — select non-archived sessions older than retain_days, write to `~/.local/share/wtfrc/archive/sessions-YYYY-MM.jsonl`, mark archived in DB.
- [ ] **Step 4: Run tests, verify PASS**
- [ ] **Step 5: Commit**: `feat: add session archiver with JSONL export and retention`

### Task 26: Bubble Tea REPL — Styles + Input Component

**Files:**
- Create: `internal/tui/styles.go`
- Create: `internal/tui/components/input.go`
- Create: `internal/tui/components/answer.go`

- [ ] **Step 1: Create styles.go** — Lip Gloss theme for the popup: header style, input prompt style, answer body style, source reference style. Use the ricing aesthetic — clean borders, subtle colors.

- [ ] **Step 2: Create input.go** — Wrap `bubbles/textinput` with history (arrow up/down through previous queries in session). Expose `Value()`, `SetValue()`, `HistoryUp()`, `HistoryDown()`.

- [ ] **Step 3: Create answer.go** — Wrap `bubbles/viewport` for scrollable streaming answer. Accept streamed tokens via a method, render markdown with Glamour. Show source references at the bottom.

- [ ] **Step 4: Verify compilation**: `go build ./internal/tui/...`
- [ ] **Step 5: Commit**: `feat: add TUI styles and input/answer components`

### Task 27: Bubble Tea REPL — Main Model

**Files:**
- Create: `internal/tui/ask.go`

The main Bubble Tea model ties everything together:

- [ ] **Step 1: Implement ask.go**

States: `stateInput` (waiting for query), `stateLoading` (spinner while searching KB), `stateStreaming` (streaming answer tokens), `stateAnswer` (answer complete, waiting for next query or exit).

Key behavior:
- On Enter in stateInput: search KB, build LLM prompt, stream response
- On Esc or Ctrl+C: quit
- On arrow up/down in stateInput: history navigation
- KB search and LLM streaming happen via Bubble Tea commands (Cmd)

The Model struct MUST hold: an Input component, an Answer component, a spinner, the current state, a session Manager reference, the current session ID, a DB reference, an LLM Provider reference, a Config reference, a query history slice, and an error field.

- [ ] **Step 2: Wire up Init, Update, View**

- `Init()`: focus text input, start session
- `Update()`: handle key events, KB search results, LLM stream tokens
- `View()`: render header + answer viewport + input prompt + source refs

- [ ] **Step 3: Manual test**: `go run ./cmd/wtfrc ask` (needs Ollama running)
- [ ] **Step 4: Commit**: `feat: add Bubble Tea ask REPL with streaming answers`

---

## Chunk 7: CLI Commands + Supervisor

### Task 28: CLI Commands

**Files:**
- Create: `internal/cli/ask.go`
- Create: `internal/cli/index.go`
- Create: `internal/cli/search.go`
- Create: `internal/cli/list.go`
- Create: `internal/cli/stats.go`
- Create: `internal/cli/doctor.go`
- Create: `internal/cli/config_cmd.go`
- Create: `internal/cli/supervise.go`

- [ ] **Step 1: Implement ask.go** — launches Bubble Tea program. Accepts optional positional arg for one-shot mode (`wtfrc ask "query"` answers then exits).

- [ ] **Step 2: Implement index.go** — `wtfrc index` (full), `--changed` (incremental), `--status` (dry run). Loads config, creates indexer, runs.

- [ ] **Step 3: Implement search.go** — `wtfrc search "query"` — FTS only, no LLM. Prints matching entries with source refs.

- [ ] **Step 4: Implement list.go** — `wtfrc list` (all entries), `--tool tmux` (filter). Table output with Lip Gloss.

- [ ] **Step 5: Implement stats.go** — entry counts by tool, session counts, query counts, average response time.

- [ ] **Step 6: Implement doctor.go** — checks: Ollama running? DB exists? Config valid? Strong LLM reachable (if configured)?

- [ ] **Step 7: Implement config_cmd.go** — `wtfrc config` opens `$EDITOR`, `wtfrc config --init` generates default.toml.

- [ ] **Step 8: Wire all commands into root.go** — add each as subcommand.

- [ ] **Step 9: Verify**: `go build ./cmd/wtfrc && ./bin/wtfrc --help` shows all subcommands.

- [ ] **Step 10: Commit**: `feat: add all CLI commands (ask, index, search, list, stats, doctor, config)`

### Task 29: Supervisor

**Files:**
- Create: `internal/supervisor/supervisor.go`
- Create: `internal/supervisor/supervisor_test.go`
- Create: `internal/supervisor/report.go`

- [ ] **Step 1: Write failing test** — seed DB with sessions containing queries. Some queries cite real entry IDs, one cites a fake ID. Run supervisor review. Verify: fake-ID query gets flagged, accuracy scores are assigned, supervisor_run is logged.

- [ ] **Step 2: Run test, verify FAIL**

- [ ] **Step 3: Implement supervisor.go**

Implement the following functions:

- **Review(ctx context.Context) (*Report, error):** Run the full review pipeline over recent sessions.
- **verifyAnswerDeterministic(q kb.Query, entries []kb.KBEntry) []string:** Tier 1 deterministic check.
- **verifyAnswerLLM(ctx context.Context, q kb.Query, entries []kb.KBEntry) (*LLMVerification, error):** Tier 2 LLM cross-check.

**Two-tier hallucination detection (§8.5):**

*Tier 1 — Deterministic (runs on every query):*
- Load entries_used IDs from each query
- Verify they exist in DB
- Extract keybind/alias patterns from answer text (regex for patterns like `$mod+...`, backtick-quoted strings)
- Cross-check against cited entries' raw_binding/raw_action

*Tier 2 — LLM cross-check (runs on ambiguous cases):*
- When deterministic check is inconclusive OR entries_used is empty but answer cites specific config values
- Send question + answer + KB entries to Strong LLM with the verification prompt from spec §8.5
- Parse JSON response with fields: accurate (bool), hallucinated_refs (array), contradictions (array)
- Uses `CompleteJSON` helper for structured output

- [ ] **Step 4: Implement report.go** — generates markdown report with KPIs table, flagged queries, suggestions.

- [ ] **Step 5: Run tests, verify PASS**

- [ ] **Step 6: Implement supervise.go CLI command** — `wtfrc supervise` runs review, `wtfrc supervise --report` shows last report.

- [ ] **Step 7: Commit**: `feat: add supervisor with hallucination detection and report generation`

---

## Chunk 8: Distribution + README

### Task 30: GoReleaser Config

**Files:**
- Create: `.goreleaser.yml`

- [ ] **Step 1: Create .goreleaser.yml**

Create a GoReleaser v2 config with `project_name: wtfrc`. The build MUST target `./cmd/wtfrc` with `CGO_ENABLED=0`, building for linux and darwin on amd64 and arm64. Use ldflags to strip debug info and inject the version. Archives SHOULD use tar.gz format with a name template of `projectname_os_arch`. Configure a Homebrew tap pointing to `shaiknoorullah/homebrew-tap`. Configure an AUR package `wtfrc-bin` with MIT license and AUR_KEY from environment. Include a checksum file and ascending changelog.

- [ ] **Step 2: Create scripts/install.sh** — curl installer that detects OS/arch, downloads the correct binary from GitHub releases, installs to `~/.local/bin/`.

- [ ] **Step 3: Create Makefile release target**

Add a `release` target to the Makefile that runs `goreleaser release --clean`.

- [ ] **Step 4: Commit**: `feat: add GoReleaser config and curl installer`

### Task 31: `wtfrc setup` Command

**Files:**
- Create: `internal/cli/setup.go`

The first-run experience. `wtfrc setup` auto-detects config files, generates a default `~/.config/wtfrc/config.toml`, optionally pulls the default Ollama model, and runs the initial index.

- [ ] **Step 1: Implement setup.go**

Create a Cobra command that performs the following steps in order:

1. Create the `~/.config/wtfrc/` directory if it does not exist.
2. Write `configs/default.toml` to `~/.config/wtfrc/config.toml` if it does not already exist.
3. Scan `~/.config/` for known config patterns and add discovered paths to the config.
4. Check Ollama health. If not running, print setup instructions.
5. Optionally pull the default model (`ollama pull gemma3:4b`) using a `huh.NewConfirm()` interactive prompt.
6. Run the initial index by calling `indexer.Index`.
7. Print a success message with a suggested first command: `wtfrc ask 'how do I close a window'`.

Key behavior:
- Uses `huh.NewConfirm()` for interactive prompts (pull model yes/no)
- MUST be idempotent — safe to re-run (skips steps already done)
- Shows progress with `charmbracelet/log`

- [ ] **Step 2: Wire into root.go**: `rootCmd.AddCommand(setupCmd)`

- [ ] **Step 3: Manual test**: `go run ./cmd/wtfrc setup`

- [ ] **Step 4: Commit**: `feat: add wtfrc setup command for first-run experience`

---

### Task 32: README

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Write README** with:
- Hero section with project name and tagline
- Placeholder for VHS-generated hero GIF (`<!-- TODO: Record with VHS -->`)
- Feature highlights (local-first, LLM-agnostic, privacy, offline)
- Quick install section (curl, go install, AUR, Homebrew)
- Quick start (`wtfrc index && wtfrc ask`)
- Window manager integration snippets (i3, Hyprland)
- Config section
- Roadmap (v0.2 Coach, v1.0 Train)
- Match the brand voice from brainstorming — direct, slightly irreverent, technically credible

- [ ] **Step 2: Add LICENSE** (MIT)

- [ ] **Step 3: Commit**: `docs: add README and MIT license`

### Task 33: Final Integration Test

- [ ] **Step 1: Run full test suite**: `go test ./... -v -count=1`
Expected: All tests PASS

- [ ] **Step 2: Build binary**: `go build -o bin/wtfrc ./cmd/wtfrc`
Expected: Clean build, zero warnings

- [ ] **Step 3: Run doctor**: `./bin/wtfrc doctor`
Expected: Shows health check results (Ollama may not be running — that's OK)

- [ ] **Step 4: Run help**: `./bin/wtfrc --help`
Expected: All subcommands listed

- [ ] **Step 5: Commit any final fixes**

---

## Task Dependencies

```
Task 1 (scaffold) → Task 2 (config) → Task 3 (KB schema)
Task 3 → Task 4 (FTS search)
Task 5 (parser interface) → Tasks 6-9 (first 4 parsers) → Tasks 10-15 (remaining parsers)
Task 16 (redactor) — independent of parsers
Task 17 (LLM types) → Tasks 18-19 (providers) → Task 20 (CompleteJSON) → Task 20b (fallback)
Task 3 + Task 5 + Task 16 → Task 21 (manifest) → Task 22 (indexer) → Task 23 (enricher)
Task 3 → Task 24 (session mgr) → Task 25 (archiver)
Task 4 + Task 18 + Task 20b + Task 24 → Tasks 26-27 (TUI)
Task 22 + Task 27 → Task 28 (CLI commands)
Task 3 + Task 4 + Task 24 + Task 20 → Task 29 (supervisor)
Task 28 + Task 22 → Task 30 (distribution)
Task 28 → Task 31 (setup command)
Task 30 → Task 32 (README)
All → Task 33 (integration)
```

**Parallelizable groups:**
- Tasks 6, 7, 8, 9 (parsers — all independent)
- Tasks 10, 11, 12, 13, 14, 15 (more parsers — all independent)
- Task 16 (redactor) parallel with Tasks 17-20b (LLM layer)
- Task 24-25 (session) parallel with Task 21-23 (indexer) once Task 3 is done
